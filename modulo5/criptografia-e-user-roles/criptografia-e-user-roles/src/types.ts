export type user = {
    id: string
    email: string
    hashPassword: string
    name: string
    nickname: string,
    role: string
 }
 
 export type AuthenticationData = {
    id: string,
    role: string
 }