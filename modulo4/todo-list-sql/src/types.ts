export type body = {
    id: string,
    name: string,
    nickname: string,
    email: string
}

export type bodyTask = {
    id: string,
    title: string,
    description: string,
    limitDate: string,
    creatorUserId: string
}